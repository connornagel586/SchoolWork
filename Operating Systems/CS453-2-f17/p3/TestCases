Test Cases



    ~~~~~Test Cases~~~~		~~~~~~~Result~~~~~~~

	cd <path>		    switches cwd
	
	pwd			    returns p3 directory contents

	cd mydash-src pwd	    returns mydash-src contents

	cd ..			    changes to parent directory
	
	touch <name>		    creates a new file

	sleep 10&		    starts sleep in background

	sleep 10 &		    starts sleep in background

	sleep 10     &   	    starts sleep in background

	clear			    clears shell output

	vim mydash.c		    opens file in vim

	./mydash -v		    displays version

	EOF signal		    exits mydash

	exit			    exits mydash

	loop then ^Z                stop loop process

	loop then ^C		    interrupt loop process

	./mydash		    opens new shell as a process

	sleep 10 & 		    removes trailing space and executes

	^Z			    Ignores signal
	
	^C			    Ignores signal

	""-empty string	   	    Returns new prompt
